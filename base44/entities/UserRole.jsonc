{
  "name": "UserRole",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "User ID reference"
    },
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "host",
        "cleaner",
        "guest"
      ],
      "description": "User role"
    },
    "approval_status": {
      "type": "string",
      "enum": [
        "pending",
        "approved",
        "rejected"
      ],
      "default": "pending",
      "description": "Role approval status"
    },
    "messaging_disabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether messaging is disabled for this role"
    },
    "stripe_connect_account_id": {
      "type": "string",
      "description": "Stripe Connect Express account ID"
    },
    "stripe_connect_status": {
      "type": "string",
      "enum": [
        "not_connected",
        "pending",
        "pending_verification",
        "verified"
      ],
      "default": "not_connected",
      "description": "Stripe Connect onboarding status"
    }
  },
  "required": [
    "user_id",
    "role"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}